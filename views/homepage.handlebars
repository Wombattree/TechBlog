{{#each blogPosts as |blogPost|}}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2><a href="/blogPost/{{blogPost.id}}">{{blogPost.title}}</a></h2>
  </div>
  <div class="col-md-7">
    <p>{{blogPost.body}}</p>
    <p>By: {{blogPost.user.name}}</p>
  </div>
</div>
{{/each}}